    n_rows, n_cols = detectar_dimensiones(base_file)
    print(f"🔍 Matriz detectada: {n_rows}×{n_cols}")

    def full(flag):
        return {f"R{i}{j}_1": flag for i in range(1, n_rows+1) for j in range(1, n_cols+1)}

    combos = [
        full("VcON"),
        #full("VcMAS"),
        #full("VcMENOS"),
    ]
    alt = {f"R{i}{j}_1": ("VcON" if (i+j) % 2 else "VcOFF") for i in range(1, n_rows+1) for j in range(1, n_cols+1)}

    combos.append(alt)
    return combos, n_rows, n_cols





